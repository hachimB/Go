// byte = uint8 - 2^8 -1 => max = 255
// rune = int32 : Typically used for Unicode characters since Unicode code points are 32-bit.
// fmt.Printf("The binary representation is %b", variableName)
// fmt.Printf("The scientific representation is %e", variableName)
// fmt.Printf("The value is %v", variableName)

package main

// func greeting(name string) string {
// 	greet := fmt.Sprintf("Hello %s", name)
// 	return greet
// }

func main() {
	/* num := 90
	x := fmt.Sprintf("The number is %d and it's Type is %T", num, num)
	fmt.Println(x)
	fmt.Println(greeting("Ali"))
	**/

	// fmt.Println(Sum(29, 30))

	/* fmt.Printf("The max between 9 and 30 is %f\n", math.Max(9, 30))
	fmt.Printf("The min between 7 and 70 is %f\n", math.Min(7, 70))
	**/

	/* y := "1011"
	fmt.Println(strconv.Atoi(y))
	val, err := strconv.ParseInt(y, 10, 0)
	fmt.Println(val, err)
	**/

	// str()

	// array()

	// slice()

	// maps()

	/**Functions
	fmt.Println(myFunc(f))

	val := myFunc(func(num int) int {
		return num * 3
	})
	fmt.Println(val)

	f1 := concatStr("Hello")
	val3 := f1("world!")

	fmt.Println(val3)

	fmt.Println(sum(12, 2, 3, 3, 4, 45))
	fmt.Println(mul(7, 3, 3))
	*/

	/**
	Struct:
	structure()
	*/

	/*intrfce()**/

	/*Errors
	errorHandlingWithPanic()
	errorHandler()
	**/

	/*
		generic()
		**/

	pointer()
}
